name: infinite_cycle_carousel_example
description: Example app for infinite_cycle_carousel package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.9.3
  infinite_cycle_carousel:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
